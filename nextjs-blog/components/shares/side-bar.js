import React from 'react';
import PropTypes from 'prop-types';

const SideBar = ({}) => {
    return (
        <div>
            Side Bar
        </div>
    );
};

SideBar.propTypes = {

};

export default SideBar;
